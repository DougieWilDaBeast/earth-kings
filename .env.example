# Optional: only for the LLM gateway (github.com/DougieWilDaBeast/llm-gateway).
# The game never reads these. Copy to .env (gitignored) for tools/map_voice_note.py.
LLM_GATEWAY_URL=http://localhost:4000
LLM_GATEWAY_KEY=sk-same-as-gateway-LITELLM_MASTER_KEY
